---
# tasks file for docker
- name: Ensure pip is install
  apt:
    update-cache: yes
    name: python-pip
    state: present

- name: Ensure docker is install
  pip: 
    name: docker
    state: present